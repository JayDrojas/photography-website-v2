import { Box } from '@chakra-ui/react';
import { NavbarProps } from '../NavbarTypes';

const MobileNavbar = ({ navItems }: NavbarProps) => {
  return <Box>Mobile Navbar</Box>;
};

export default MobileNavbar;
