query GetPage($slug: String = "") {
  pageCollection(where: { slug: $slug }, limit: 1) {
    items {
      pageTitle
      slug
      sys {
        id
      }
      sectionsCollection {
        items {
          title
          backgroundImage {
            title
            url
          }
        }
      }
    }
  }
}
